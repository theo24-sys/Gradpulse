{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "python manage.py collectstatic --noinput && gunicorn gradpulse.wsgi:application --log-file -",
    "releaseCommand": "python manage.py migrate --noinput"
  }
}
